{
  "devDependencies": {
    "cypress": "^13.6.1",
    "cypress-mochawesome-reporter": "^3.7.0",
    "cypress-plugin-api": "^2.11.1"
  },
  "scripts": {
    "test": "cypress open",
    "test-headless": "cypress run --browser chrome --headless --spec cypress/e2e/publicapisAPI.cy.js"
  }
}
